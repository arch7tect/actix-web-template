apiVersion: v1
kind: Service
metadata:
  name: memos-app
  namespace: memos
  labels:
    app: memos-app
    app.kubernetes.io/part-of: memos
    app.kubernetes.io/component: app
spec:
  type: NodePort
  selector:
    app: memos-app
  ports:
    - port: 3737
      targetPort: 3737
      nodePort: 30080
