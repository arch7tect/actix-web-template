apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: memos
  labels:
    app: postgres
    app.kubernetes.io/part-of: memos
    app.kubernetes.io/component: database
spec:
  type: ClusterIP
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
